version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./comercio_exterior.db:/app/comercio_exterior.db
    environment:
      - DATABASE_URL=sqlite:///./comercio_exterior.db
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload